import 'package:flutter/material.dart';

class Raceregister extends StatelessWidget {
  const Raceregister({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}